# Ansible Automation

Ansible examples for repeatable Linux configuration.

## Structure
- `inventory.ini` - example inventory
- `site.yml` - main playbook
- `roles/` - reusable role structure

Do not commit real hostnames, private IPs, passwords or SSH keys.
